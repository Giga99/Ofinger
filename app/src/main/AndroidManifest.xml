<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ofinger">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.FullScreen">
        <activity android:name=".settings.HelpActivity"/>
        <activity android:name=".settings.DeletingAccountActivity" />
        <activity android:name=".settings.AboutActivity" />
        <activity android:name=".navigationActivities.WishListActivity" />
        <activity android:name=".navigationActivities.NotificationActivity" />
        <activity android:name=".settings.EditNotificationsActivity" />
        <activity android:name=".settings.EditEmailActivity" />
        <activity android:name=".startActivities.EmailVerificationActivity" />
        <activity android:name=".settings.EditProfileActivity" />
        <activity android:name=".settings.EditPassActivity" />
        <activity android:name=".settings.SettingsActivity" />

        <service
            android:name=".notifications.NotificationService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".customDialogs.CustomDialogZoomImage" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".messaging.ChatActivity" />
        <activity android:name=".info.ClothInfo" />
        <activity android:name=".adding.AddingCloth" />
        <activity android:name=".mainActivities.MainActivity" />
        <activity android:name=".startActivities.ResetPasswordActivity"
            android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".startActivities.LoginActivity"
            android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".startActivities.RegisterActivity"
            android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".startActivities.StartActivity"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.ofinger.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/paths" />
        </provider>

        <meta-data
            android:name="firebase_performance_logcat_enabled"
            android:value="true" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher_background" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
    </application>

</manifest>